---
label: page-row
hide_body: true
fields:
- name: columns-row
  type: field_group_list
  config:
    min: 
    max: 
    labelField: 
  label: Columns-row
  description: a template for columns describing products
- name: single_product_info
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: false
    schema:
      format: markdown
  label: Single product info
  description: a single product info in a list
