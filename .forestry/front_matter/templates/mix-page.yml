---
label: mix page
hide_body: true
fields:
- name: the_label
  type: text
  config:
    required: false
  label: the label
  description: my first field
- name: a_block
  type: blocks
  label: a block
  template_types: []
  config:
    min: 
    max: 
  description: my first block
- name: this_should_be_an_image
  type: file
  config:
    maxSize: 64
  label: This should be an image
  default: "/uploads/obama.jpg"
  description: An image of Obama
