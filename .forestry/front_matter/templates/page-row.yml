---
label: page-row
hide_body: true
fields:
- name: columns-row
  type: field_group_list
  fields:
  - name: single_product_sintesis
    type: blocks
    label: single product sintesis
    template_types:
    - a-template-for-single-product
    config:
      min: 
      max: 
  - name: another_single_product
    type: blocks
    label: another single product
    template_types:
    - a-template-for-single-product
    config:
      min: 
      max: 
  config:
    min: 
    max: 
    labelField: 
  label: Columns-row
  description: a template for columns describing products
pages:
- content/Products/mixes/tommarola.md
