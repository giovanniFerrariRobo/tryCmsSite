---
label: mix page
hide_body: true
fields:
- name: a_block
  type: blocks
  label: a block
  template_types: []
  config:
    min: 
    max: 
  description: my first block
- name: the_label
  type: text
  config:
    required: false
  label: the label
  description: my first field
